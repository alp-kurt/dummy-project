using UnityEngine;

namespace Scripts
{
    [CreateAssetMenu(fileName = "BoltConfig", menuName = "Configs/Projectiles/Bolt")]
    public sealed class BoltConfig : ProjectileConfig
    {
        // Add Bolt-only tunables later (e.g., ricochetBias, maxPierces, etc.)
    }
}
